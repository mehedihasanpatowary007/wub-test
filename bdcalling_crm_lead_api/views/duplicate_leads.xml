

<!-- check similer lead by one phone or id number -->
<odoo>
    <record id="crm_lead_duplicate_form_button" model="ir.ui.view">
        <field name="name">crm.lead.form.duplicate.button</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//div[@name='button_box']" position="inside">

                <button name="action_view_duplicates"
                        type="object"
                        class="oe_stat_button"
                        icon="fa-copy"
                        invisible="duplicate_count == 0">

                    <div class="o_stat_info">
                        <span class="o_stat_value">
                            <field name="duplicate_count"/>
                        </span>
                        <span class="o_stat_text">
                            Duplicate Leads
                        </span>
                    </div>

                </button>

            </xpath>

        </field>
    </record>
</odoo>